# AI Meeting Assistant — HF Inference API (Serverless)
gradio>=4.44.1
starlette>=0.27.0,<0.38.0
jinja2>=3.1.2,<3.2.0

# HF Inference API client
huggingface-hub>=0.26.0

# LangChain — prompt templates only (no model loading)
langchain>=0.1.0
langchain-core>=0.1.0

# Audio processing (used by Gradio Audio component)
librosa>=0.10.0
soundfile>=0.12.0
ffmpeg-python>=0.2.0
